# 2026-04-16 运行深度分析（业务日 05:00-次日05:00）

## 一、结论摘要
2026-04-16 的系统运行整体稳定，采集链路与消息链路均实现了全天持续输出，满足“次日可分析”的基本要求。  
从数据量看，快照与消息均达到可复盘规模；从稳定性看，核心异常集中在少量 SQLite 锁冲突与风控受限场景，未出现长时间不可用。

## 二、数据规模与覆盖度
本次分析窗口为 `2026-04-15T21:00:00Z` 到 `2026-04-16T21:00:00Z`（北京时间 05:00 到次日 05:00）。

- 快照总量：`19044`
- 在播快照：`6614`（约 34.7%）
- 受限快照：`1138`（约 6.0%）
- 消息总量：`9638`
- 聊天消息量（Chat）：`2307`

解读：  
1. 快照规模已足够支持按小时、按学科、按内部/竞品的统计分析。  
2. 弹幕聊天数据已形成有效样本，但仍明显低于全量消息（系统存在派生消息与状态消息），因此“话术分析”应优先基于 Chat 类型。  
3. 受限快照占比约 6%，会对个别时段和个别房间产生低估偏差，需要在报告中说明采样上下文。

## 三、时段特征（按北京时间小时）
从小时分布看，数据具有明显的“晚间高活跃”特征：

- 峰值在线人数最高时段：`21:00` 和 `22:00`（峰值均达到 `99`）
- 平均在线人数较高时段：`22:00 (15.8)`、`23:00 (13.06)`、`20:00 (10.98)`、`14:00 (10.13)`
- 低活跃时段：`02:00-04:00` 基本无在播样本

解读：  
1. 晚间（20:00-23:00）是重点投放与重点话术复用窗口。  
2. 14:00-17:00 存在次峰，可作为预热与转化承接阶段。  
3. 夜间低活跃时段可降低高频资源开销，把计算资源优先给高峰时段。

## 四、学科与竞品结构
按学科均值在线表现（从高到低）：

1. 外部数学：`20.64`（峰值 `99`）
2. 外部语文：`5.61`（峰值 `64`）
3. 小数：`4.71`（峰值 `83`）
4. 小英：`4.06`（峰值 `43`）
5. 小语：`2.92`（峰值 `37`）
6. 外部英语：`1.82`（峰值 `7`）
7. 素养：`0`（当天无在播）

按分类对比：

- 竞品：均值在线 `9.36`，峰值 `99`
- 内部：均值在线 `3.18`，峰值 `83`

解读：  
1. 竞品在当天整体在线水平显著高于内部，尤其体现在外部数学和外部语文的承载能力。  
2. 内部中表现相对较好的板块在小数与小英，但头部房间贡献明显，结构上仍偏“点状”。  
3. 素养当天无在播，相关结论需标注“样本不足”而非“效果差”。

## 五、直播间层级观察
头部房间（按均值在线）集中在：

- 刘育涛（外部数学，竞品）：均值 `20.64`，峰值 `99`
- 学而思大阅读官方旗舰店（外部语文，竞品）：均值 `11.25`，峰值 `64`
- 一鸣讲数学（小数，内部）：均值 `11.09`，峰值 `83`
- 紫一老师讲剑桥（小英，内部）：均值 `8.76`，峰值 `43`

解读：  
1. 内部并非没有高表现房间，但头部房间数量与持续性弱于竞品。  
2. 应以“头部房间节奏复制”作为短期策略，而不是平均分配优化资源。  
3. 部分账号存在名称变体（如括号符号差异）导致统计碎片化，建议后续做账号名标准化映射。

## 六、消息结构与话术分析可用性
消息类型占比呈现“派生消息 + 聊天消息并行”特征：

- DerivedOnlineDelta：`4255`
- BridgeChatMessage：`2307`
- ApiRoomPulse：`1915`
- DerivedLikeDelta：`534`
- BridgeRoomStatsMessage：`425`

解读：  
1. 弹幕分析应只取 `*Chat*`，否则会被状态类与派生类消息稀释。  
2. 当前聊天消息量足够支撑“高频词 + 时段 + 重点房间”级别分析。  
3. 若要做更细粒度语义分析，建议再提升高峰时段 chat 覆盖率。

## 七、稳定性与异常
日志统计（当天）：

- 总日志：`48754`
- `info`: `41278`，`warn`: `7472`，`error`: `4`
- 系统健康告警：`3` 次（均为 message worker 心跳年龄偏大触发）
- `database is locked` 相关：`8` 次
- 桥接上报错误：`2046` 次（多为短时链路扰动，可恢复）

当天 error 级别问题主要是：
1. 早期 cookie 未就绪导致消息 worker 无法启动（后续恢复）  
2. 少量 SQLite 锁冲突（重试后恢复）

解读：  
1. 系统没有出现长时间停摆，属于“有抖动但可恢复”。  
2. 影响稳定性的第一优先项仍是 SQLite 并发写锁。  
3. 如果后续并发继续提升，建议拆分写入或引入更强并发数据库方案。

## 八、对今天复盘可直接使用的建议
1. 复盘主看 20:00-23:00 与 14:00-17:00 两段，优先观察同学科内“头部房间节奏差”。  
2. 话术结论统一基于 Chat 消息，不混入派生消息。  
3. 报告中明确“受限快照占比约 6%”，避免对个别房间做过度确定性判断。  
4. 内部优化优先级：先复用内部头部房间节奏，再做全局扩散。  
5. 工程侧优先级：先压低 SQLite 锁冲突，再讨论更重能力（如全量录屏）。

---
该分析基于本地 `storage/app.db` 与 `data/runtime/logs/2026-04-16.log` 统计结果生成。
